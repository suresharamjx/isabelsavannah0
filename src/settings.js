let defaults = {
    food: {
        sides: 3,
        radius: 30,
        omega: .1,
        rate: 0.01,
        initial: 3,
    },

    field: {
        xSize: 3000,
        ySize: 3000,
        renderScale: 3,
    },

    physicsTickTime: 0.01,
    displayTickTime: (1/60),
}

export {defaults}
