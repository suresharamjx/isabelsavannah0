let defaults = {
    food: {
        sides: 3,
        radius: 30,
        omega: .1,
        rate: 0.01,
        initial: 3,
    },

    field: {
        xSize: 2000,
        ySize: 2000,
        renderScale: 2,
    },

    physicsTickTime: 0.01,
    displayTickTime: (1/60),
}

export {defaults}
