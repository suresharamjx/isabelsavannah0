let defaults = {
    food: {
        sides: 3,
        radius: 30,
        omega: .2,
        rate: 0.001,
        initial: 3,
    },

    field: {
        xSize: 5000,
        ySize: 5000,
        renderScale: 5,
    },

    physicsTickTime: 0.01,
    displayTickTime: (1/60),
}

export {defaults}
